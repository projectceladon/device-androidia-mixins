=== Overview

This mixin group defines the overall Audio properties for a given platform such as Audio HALs (primary, SoundTrigger, Effects, Aware ...),
Configurable AudioPolicy, Debug Tools, Treble Enable/Disable...

==== Options

--- remote
use this option to add audio related modules. It contains the components of an audio.

--- false
use this option to not include audio modules.

--- default
when not explicitly selected in mixin spec file, the default option will be used same as false.

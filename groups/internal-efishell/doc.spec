== Overview

This group is provided to add an EFI shell as part of celadon binaries 
to help developers boot to EFI shell even if support is missing in platform firmware

==== Options

--- true
use this option to add EFI shell binary. It will help to include an EFI shell as default in celadon binary zip.

--- false
use this option to exclude EFI shell.

--- default
when not explicitly selected in mixin spec file, the default option will be used same as false.


=== Overview

AVX is used to set properties "dalvik.vm.isa.x86.variant" and
"dalvik.vm.isa.x86_64.variant" to kabylake if avx is supported
by the device

==== Options

--- auto 
this option will enable avx.

--- false
this option will disable avx.

--- default
when not explicitly selected in mixin spec file, the default option will be used.
